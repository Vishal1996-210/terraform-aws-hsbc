terraform fmt
        ↓
terraform init
        ↓
terraform validate
        ↓
terraform plan
        ↓
Review plan
        ↓
terraform apply

Bootstrap → Prod = execution order that we follow manually.

Terraform automatically creates resources in the required order based on dependencies. When one resource references another resource, 
Terraform understands the dependency automatically. If Terraform cannot identify the dependency, we can use depends_on to explicitly define it.

Reference another resource → Terraform automatically understands the order.
→ Implicit dependency
Use depends_on → We manually tell Terraform the order.
→ Explicit dependency

We use Git-based workflows for infrastructure changes. Terraform plan is generated during the CI process, 
reviewed and approved through the change process, and Terraform apply is executed through the controlled deployment pipeline.

When a developer raises a PR:

Pull Request
     ↓
Checkout code
     ↓
Authenticate to AWS using OIDC
     ↓
terraform fmt -check
     ↓
terraform init
     ↓
terraform validate
     ↓
terraform plan
     ↓
Upload plan


"We manage AWS infrastructure using Terraform through GitHub Actions. When a developer raises a PR, the pipeline runs Terraform formatting, initialization, validation and plan. 
After the PR is reviewed and merged into the main branch, a production pipeline generates a fresh Terraform plan and waits for production approval. 
Once approved, Terraform apply is executed using GitHub OIDC to authenticate securely with AWS."
